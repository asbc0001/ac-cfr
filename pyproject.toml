[build-system]
requires = ["setuptools==83.0.0"]
build-backend = "setuptools.build_meta"

[project]
name = "ac-cfr"
version = "0.1.0"
description = "Counterfactual regret minimisation poker research project"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "matplotlib==3.11.1",
    "numba==0.66.0",
    "numpy==2.4.6",
    "psutil==7.2.2",
]

[project.scripts]
ac-cfr-train = "ac_cfr.cli.train:main"
ac-cfr-benchmark = "ac_cfr.cli.benchmark:main"
ac-cfr-evaluate = "ac_cfr.cli.evaluate:main"
ac-cfr-plot-results = "ac_cfr.cli.plot_results:main"

[project.optional-dependencies]
dev = [
    "build==1.5.0",
    "phevaluator==0.5.3.1",
    "pyright==1.1.411",
    "pytest==9.1.1",
    "ruff==0.15.22",
]

[tool.setuptools.packages.find]
where = ["src"]

[tool.setuptools.package-data]
"ac_cfr.games.holdem.evaluator.data" = ["*.bin", "*.json"]

[tool.pytest.ini_options]
addopts = [
    "-ra",
    "--strict-markers",
    "-m",
    "not slow and not gpu and not cloud and not benchmark",
]
testpaths = ["tests"]
markers = [
    "slow: tests that are too expensive for the default test suite",
    "gpu: tests that require a compatible GPU",
    "cloud: tests that require cloud infrastructure",
    "benchmark: performance measurements that are not correctness tests",
]

[tool.pyright]
include = ["src", "tests", "tools"]
pythonVersion = "3.12"
typeCheckingMode = "standard"
venv = ".venv"
venvPath = "."

[tool.ruff]
required-version = "==0.15.22"
target-version = "py312"
line-length = 100
src = ["src", "tests"]

[tool.ruff.lint]
select = ["E4", "E7", "E9", "F", "I", "UP", "B", "SIM"]

[tool.ruff.format]
docstring-code-format = true
